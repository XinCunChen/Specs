Pod::Spec.new do |s|
  s.name             = 'SQService'
  s.version          = '0.1.0'
  s.summary          = '网络组件'

# This description is used to generate tags and improve search results.
#   * Think: What does it do? Why did you write it? What is the focus?
#   * Try to keep it short, snappy and to the point.
#   * Write the description between the DESC delimiters below.
#   * Finally, don't worry about the indent, CocoaPods strips it!

s.description      = <<-DESC
TODO: Add long description of the pod here.
DESC

  s.homepage         = 'http://gitlab.demo580.com/ios/SQServiceComponent'
  # s.screenshots     = 'www.example.com/screenshots_1', 'www.example.com/screenshots_2'
  s.license          = { :type => 'MIT', :file => 'LICENSE' }
  s.author           = { 'XinCunChen' => 'chenxc@sq580.com' }
  s.source           = { :git => 'git@gitlab.demo580.com:ios/SQServiceComponent.git', :tag => s.version.to_s }
  # s.social_media_url = 'https://twitter.com/<TWITTER_USERNAME>'

  s.ios.deployment_target = '8.0'

  s.source_files = 'SQService/Classes/**/*'
  
  # s.resource_bundles = {
  #   'SQService' => ['SQService/Assets/*.png']
  # }

  s.public_header_files = 'SQService/Classes/**/*.h'
  s.frameworks = 'UIKit', 'Foundation','SystemConfiguration', 'MobileCoreServices'
  s.dependency 'AFNetworking', '~> 2.6.0'
  s.dependency 'YYModel'
end
